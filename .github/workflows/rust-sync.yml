name: Rust sync CI

on:
  push:
    branches:
      - master
    paths:
      - 'rust-lib-sync/**'
      - '.github/workflows/rust-sync.yml'
  pull_request:
    branches:
      - master
    paths:
      - 'rust-lib-sync/**'
      - '.github/workflows/rust-sync.yml'

defaults:
  run:
    working-directory: rust-lib-sync

jobs:
  test:
    name: Test, clippy, fmt
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - name: Format check
        run: cargo fmt --check
      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: Test
        run: cargo test
